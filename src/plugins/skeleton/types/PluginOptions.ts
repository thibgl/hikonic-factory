/**
 * Options for the Iconify plugin.
 */
export interface SkeletonPluginOptions {
  /**
   * Whether the Skeleton plugin is enabled.
   * @default true
   */
  enabled?: boolean
  /**
   * The client path for the Skeleton plugin.
   * @default './'
   */
  clientPath?: string
}
